% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/elicitation.R
\name{elicit_beta}
\alias{elicit_beta}
\title{Elicit a Beta prior via quantile matching or moment matching}
\usage{
elicit_beta(
  quantiles = NULL,
  mean = NULL,
  sd = NULL,
  method = c("quantile", "moments"),
  expert_id = "Expert_1",
  label = "Unknown quantity",
  tol = 1e-06
)
}
\arguments{
\item{quantiles}{Named numeric vector of quantile specifications, e.g.
\code{c("0.05" = 0.1, "0.50" = 0.3, "0.95" = 0.6)}. At least two quantiles
required.}

\item{mean}{Optional numeric. Expert-specified mean for moment matching.}

\item{sd}{Optional numeric. Expert-specified SD for moment matching.}

\item{method}{Character. One of \code{"quantile"} (default) or \code{"moments"}.}

\item{expert_id}{Character. Identifier for this expert's elicitation.}

\item{label}{Character. Description of the quantity being elicited.}

\item{tol}{Numeric. Optimisation tolerance. Default \code{1e-6}.}
}
\value{
An object of class \code{bayprior} with components:
\describe{
\item{\code{dist}}{\code{"beta"}}
\item{\code{params}}{Named list with \code{alpha} and \code{beta}}
\item{\code{method}}{Elicitation method used}
\item{\code{expert_id}}{Expert identifier}
\item{\code{label}}{Quantity label}
\item{\code{input}}{Raw elicitation inputs}
\item{\code{fit_summary}}{Summary statistics of fitted prior}
}
}
\description{
Fits a Beta(alpha, beta) distribution to expert-specified quantiles or
moments. Implements the structured elicitation framework recommended in
the SHELF methodology and FDA guidance on Bayesian clinical trials.
}
\examples{
# Expert believes response rate is ~30\%, with 90\% CI of [10\%, 60\%]
prior <- elicit_beta(
  quantiles = c("0.05" = 0.10, "0.50" = 0.30, "0.95" = 0.60),
  expert_id = "Expert_1",
  label = "Response rate (treatment arm)"
)
print(prior)
plot(prior)

# Moment-based elicitation
prior_mom <- elicit_beta(
  mean = 0.30, sd = 0.12,
  method = "moments",
  label = "Response rate (treatment arm)"
)

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conflict_mahalanobis.R
\name{conflict_mahalanobis}
\alias{conflict_mahalanobis}
\title{Mahalanobis-style prior-data conflict check (multivariate extension)}
\usage{
conflict_mahalanobis(
  prior_means,
  prior_cov,
  obs_means,
  obs_cov,
  alpha = 0.05,
  labels = NULL
)
}
\arguments{
\item{prior_means}{Numeric vector. Prior means for each parameter.}

\item{prior_cov}{Numeric matrix. Prior covariance matrix
(p × p, positive definite).}

\item{obs_means}{Numeric vector. Observed data means (same length as
\code{prior_means}).}

\item{obs_cov}{Numeric matrix. Observed covariance matrix of the
sample means (often \code{Sigma_data / n}).}

\item{alpha}{Numeric. Significance level for the conflict flag.
Default \code{0.05}.}

\item{labels}{Character vector. Names of the parameters.}
}
\value{
A list of class \code{bayprior_conflict_mv} with:
\describe{
\item{\code{mahal_distance}}{Mahalanobis distance.}
\item{\code{pvalue}}{p-value under chi-squared distribution.}
\item{\code{df}}{Degrees of freedom (= number of parameters).}
\item{\code{conflict_flag}}{Logical.}
\item{\code{interpretation}}{Plain-language summary.}
}
}
\description{
Extends the standard scalar conflict diagnostics to the multivariate
setting where the prior simultaneously specifies beliefs about multiple
parameters. The Mahalanobis distance between the prior location and the
observed data vector provides a single omnibus conflict statistic with
a known chi-squared reference distribution under compatibility.
}
\details{
For the univariate case this reduces to the squared surprise index from
\code{prior_conflict()}, but the function also supports a two-endpoint version
useful when a primary and key secondary endpoint are both modelled
simultaneously (common in oncology trials).
}
\examples{
# Two endpoints: response rate and OS rate
prior_means <- c(0.35, 0.60)
prior_cov   <- matrix(c(0.01, 0.003, 0.003, 0.015), 2, 2)
obs_means   <- c(0.55, 0.58)
obs_cov     <- matrix(c(0.008, 0.002, 0.002, 0.010), 2, 2) / 50

mv_check <- conflict_mahalanobis(
  prior_means, prior_cov, obs_means, obs_cov,
  labels = c("Response rate", "OS rate")
)
print(mv_check)

}

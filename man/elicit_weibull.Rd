% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/elicitation.R
\name{elicit_weibull}
\alias{elicit_weibull}
\title{Elicit a Weibull prior via moments, direct parameters, or quantile matching}
\usage{
elicit_weibull(
  shape = NULL,
  scale = NULL,
  mean = NULL,
  sd = NULL,
  quantiles = NULL,
  method = c("moments", "params", "quantile"),
  expert_id = "Expert",
  label = "Quantity"
)
}
\arguments{
\item{shape}{Numeric > 0. Shape parameter \eqn{k}. Used when
\code{method = "params"}.}

\item{scale}{Numeric > 0. Scale parameter \eqn{\lambda}. Used when
\code{method = "params"}.}

\item{mean}{Numeric > 0. Prior mean. Used with \code{sd} when
\code{method = "moments"}.}

\item{sd}{Numeric > 0. Prior SD. Used with \code{mean} when
\code{method = "moments"}.}

\item{quantiles}{Named numeric vector with at least two interior quantiles.
Used when \code{method = "quantile"}.}

\item{method}{Character. One of \code{"moments"}, \code{"params"}, or
\code{"quantile"}. Default \code{"moments"}.}

\item{expert_id}{Character. Identifier for the eliciting expert.}

\item{label}{Character. Human-readable label for the quantity.}
}
\value{
A \code{bayprior} object with \code{dist = "weibull"}.
}
\description{
Fits a Weibull(shape, scale) prior from expert-specified moments or
quantiles. The Weibull distribution generalises the Exponential and is
widely used for survival analysis with non-constant hazard.
}
\details{
Parameterised as in R's \code{stats::dweibull}: shape \eqn{k} and
scale \eqn{\lambda}, with mean \eqn{\lambda \Gamma(1 + 1/k)} and
variance \eqn{\lambda^2 [\Gamma(1 + 2/k) - \Gamma(1 + 1/k)^2]}.

\strong{Shape parameter interpretation:}
\itemize{
\item \eqn{k < 1}: decreasing hazard (e.g. early mortality selecting out)
\item \eqn{k = 1}: constant hazard (reduces to Exponential)
\item \eqn{k > 1}: increasing hazard (e.g. ageing, post-surgical)
}
}
\examples{
# Moment matching: mean 20 months, SD 10 months
p <- elicit_weibull(mean = 20, sd = 10, method = "moments",
                     label     = "Survival time (months)",
                     expert_id = "Expert_1")
print(p)
plot(p)

# Direct parameters (shape = 2 = increasing hazard)
p2 <- elicit_weibull(shape = 2, scale = 20, method = "params",
                      label = "PFS (months)")

# Quantile matching (at least 2 required)
p3 <- elicit_weibull(
  quantiles = c("0.10" = 5, "0.50" = 18, "0.90" = 40),
  method    = "quantile",
  label     = "OS (months)"
)

}

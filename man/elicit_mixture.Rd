% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/elicitation.R
\name{elicit_mixture}
\alias{elicit_mixture}
\title{Elicit a mixture prior}
\usage{
elicit_mixture(components, weights = NULL, label = "Mixture prior")
}
\arguments{
\item{components}{List of \code{bayprior} objects (all same distribution family).}

\item{weights}{Numeric vector of mixing weights (must sum to 1). If \code{NULL},
equal weights are assigned.}

\item{label}{Character. Label for the mixture prior.}
}
\value{
A \code{bayprior} object with \code{dist = "mixture"}.
}
\description{
Constructs a finite mixture prior from a list of component \code{bayprior}
objects (e.g., from \code{elicit_beta}, \code{elicit_normal}). Mixing weights can
be specified or estimated via linear pooling.
}
\examples{
p1 <- elicit_beta(mean = 0.2, sd = 0.08, method = "moments", expert_id = "E1")
p2 <- elicit_beta(mean = 0.4, sd = 0.10, method = "moments", expert_id = "E2")
mix <- elicit_mixture(list(p1, p2), weights = c(0.5, 0.5), label = "Pooled prior")

}
